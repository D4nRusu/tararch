#include <stdio.h>
#include "arch.h"

int main(int argc, char** argv)
{
    // FILE* f = fopen("testfile.txt", "rb");

    // struct posix_header h = generateHeader(f, argv[1]);
    // printf("%s %s %s %s\n", h.name, h.mode, h.uid, h.gid);

    int a = 1;
    printf("%c\n", a);
    return 0;
}